given a linked list of n nodes such that it may contain a loop a loop here means that the last node of the link list is connected to the node at position x(1-based index).if the link list does not have any loop x=0.remove the loop from the linked list if it is present ie unlink the last node which is forming the loop
