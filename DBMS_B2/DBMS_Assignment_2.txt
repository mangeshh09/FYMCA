ASSIGNEMENT 1_A

CREATE TABLE EMPLOYEE(
ID INT,
PERSON_NAME VARCHAR(20),
STREET VARCHAR(30),
CITY VARCHAR(30),
PRIMARY KEY (ID)
);

CREATE TABLE WORKS(
ID INT,
COMPANY_NAME VARCHAR(20),
PRIMARY KEY (ID)
);

CREATE TABLE COMPANY(
COMPANY_NAME VARCHAR(20),
CITY VARCHAR(30),
PRIMARY KEY (COMPANY_NAME)
);

CREATE TABLE MANAGES(
ID INT,
MANAGER_ID INT,
PRIMARY KEY (ID)
);

DESC CMP   ;

ALTER TABLE WORKS ADD SALARY INT;  

ALTER TABLE EMPLOYEE DROP COLUMN STREET;  

ALTER TABLE MANAGES RENAME COLUMN MANAGER_ID to MANAGER;    

RENAME COMPANY To CMP;    

DROP TABLE "MANAGES";  



CREATE TABLE EMPLOYEE1(
EMPNO INT,
EMP_NAME VARCHAR(20),
DESIGNATION VARCHAR(30),
CITY VARCHAR(30),
SALARY INT,
ZIPCODE INT,
COUNTRY VARCHAR(30)
);

DESC EMPLOYEE1;

CREATE SEQUENCE emp_seq
START WITH 1
INCREMENT BY 1;

CREATE INDEX idx_county ON EMPLOYEE1(COUNTRY);


CREATE VIEW low_salary_mumbai_employees AS
SELECT EMPNO , EMP_NAME , DESIGNATION, CITY , SALARY , ZIPCODE , COUNTRY 
FROM EMPLOYEE1
WHERE SALARY < 50000 AND CITY = 'Mumbai';


ASSIGNEMENT 1_B
CREATE TABLE EMPLOYEEDETAILS(
EMPNO INT, EMPNAME VARCHAR (20), DESIGNATION VARCHAR(20), CITY VARCHAR(20), SALARY INT, ZIPCODE INT, COUNTRY VARCHAR (20));

DESC EMPLOYEEDETAILS;

INSERT INTO EMPLOYEEDETAILS (EMPNO, EMPNAME, DESIGNATION, CITY, SALARY, ZIPCODE, COUNTRY)
VALUES('24', 'MANGESH', 'MANAGER', 'NASHIK', '400000', '422009','INDIA');

INSERT INTO EMPLOYEEDETAILS(EMPNO, EMPNAME, DESIGNATION, CITY, SALARY, ZIPCODE, COUNTRY)
VALUES('26', 'AKHIL','PEON', 'OZAR', '50000', '456890', 'AFRICA');

INSERT INTO EMPLOYEEDETAILS (EMPNO, EMPNAME, DESIGNATION, CITY, SALARY, ZIPCODE, COUNTRY)
VALUES('40', 'NEHA', 'JRDEVELOPER, MUMBAI, 45000', '780890', 'USA');

SELECT * FROM EMPLOYE EDETAILS;

CREATE SEQUENCE SEQ 1
START WITH 50
INCREMENT BY 1;

INSERT INTO EMPLOYEEDETAILS
VALUES (SEQ_1. NEXTVAL, 'JIYA', 'SRDEVELOPER', 'PUNE', '50000', '456890', 'AFRICA');

CREATE VIEW SALARY1 AS
SELECT SALARY, CITY FROM EMPLOYEEDETAILS
WHERE SALARY<50000 AND CITY='MUMBAI';

SELECT * FROM SALARY1;

CREATE INDEX CITYX
ON EMPLOYEEDETAILS(COUNTRY);

SELECT * FROM EMPLOYEEDETAILS;
